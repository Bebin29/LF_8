# SMTP-Credentials für den Alarm-E-Mail-Versand
# Kopiere diese Datei nach .env und trage die echten Werte ein:
#   cp .env.example .env
#
# WICHTIG: .env wird NICHT ins Repository committed (.gitignore)

SMTP_USERNAME=monitor@example.com
SMTP_PASSWORD=changeme
